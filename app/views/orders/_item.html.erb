<tr>
	<td><%= f.collection_select :product_id, Product.all, :id, :name, {include_blank: 'Choose product..'}, {class: 'form-control'}%></td>
	<td></td>
	<td><%= f.number_field :qty, class: 'form-control', required: true%></td>
	<td></td>
	<td>
		<%= f.input_field :_destroy, as: :hidden %>
    	<%= link_to 'Delete', '#', class: 'remove_record' %>
	</td>
</tr>